# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Node
node_modules/
dist/
.eslintcache

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Local config (secrets)
server/config.local.toml
# Local gunicorn overrides (machine-specific paths, e.g. TLS cert/key)
server/gunicorn.conf.py

# Logs. config.settings.production routes the root logger to a rotating file in
# LOGS_FOLDER, which defaults to the working directory.
django.log*

# Environment
.env.local
.env.*.local

# Agent process docs (gitignored, see docs/temp/)
docs/temp/
