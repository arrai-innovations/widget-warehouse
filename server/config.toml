# Shared config (safe to commit).
#
# These defaults target local development. Override in config.local.toml or
# a deployment-specific config layer as needed.

ALLOWED_HOSTS = ["localhost", "127.0.0.1", "astute.arrai.com"]
TIME_ZONE = "UTC"

FRONTEND_DOMAIN = "https://astute.arrai.com:8080"
FRONTEND_LOGIN_URL = "/login"

SITE_NAME = "Widget Warehouse"
SUPPORT_EMAIL = "dev@localhost"

AUTH_USER_MODEL = "users.User"
LOCAL_APPS = ["widget_warehouse.users"]

STATIC_ROOT = "/tmp/static"
STATICFILES_DIRS = []

CSRF_TRUSTED_ORIGINS = ["https://astute.arrai.com:8080", "http://localhost:8080"]
CORS_ALLOWED_ORIGINS = ["https://astute.arrai.com:8080", "http://localhost:8080"]

DATABASE_BACKUP_DIR = "/tmp/db-backups"
