[tool.uv.workspace]
members = ["server"]

[project]
name = "widget-warehouse-workspace"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "md-toc >=8.2.2",
]

[tool.uv]
default-groups = []
exclude-newer = "1d"
